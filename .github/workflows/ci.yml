name: Integracion Continua

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ main ]

jobs:
  verificar:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v3

      - name: Mensaje de verificacion
        run: echo "El flujo de integracion continua funciona correctamente"